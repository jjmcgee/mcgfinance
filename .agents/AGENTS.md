# Project Rules and Guidelines

- The frontend/application for this project runs inside a Docker container.
- There is no Node.js, npm, or npx installed locally on the host machine.
- Do not attempt to run local terminal commands like `npm run build`, `npm run dev`, or `npx` directly on the host machine.
