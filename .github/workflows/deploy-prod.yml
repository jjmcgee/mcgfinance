name: Deploy Production

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: deploy-production
  cancel-in-progress: true

jobs:
  deploy:
    name: Deploy To Ubuntu
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Start SSH agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.PROD_SSH_KEY }}

      - name: Validate deploy secrets
        env:
          PROD_SSH_HOST: ${{ secrets.PROD_SSH_HOST }}
          PROD_SSH_USER: ${{ secrets.PROD_SSH_USER }}
          PROD_SSH_PORT: ${{ secrets.PROD_SSH_PORT }}
          PROD_SSH_KNOWN_HOSTS: ${{ secrets.PROD_SSH_KNOWN_HOSTS }}
        run: |
          set -euo pipefail
          [ -n "$PROD_SSH_HOST" ] || { echo "Missing required secret: PROD_SSH_HOST"; exit 1; }
          [ -n "$PROD_SSH_USER" ] || { echo "Missing required secret: PROD_SSH_USER"; exit 1; }
          [ -n "$PROD_SSH_PORT" ] || { echo "Missing required secret: PROD_SSH_PORT"; exit 1; }
          [ -n "$PROD_SSH_KNOWN_HOSTS" ] || { echo "Missing required secret: PROD_SSH_KNOWN_HOSTS"; exit 1; }

      - name: Configure known_hosts
        env:
          PROD_SSH_KNOWN_HOSTS: ${{ secrets.PROD_SSH_KNOWN_HOSTS }}
        run: |
          set -euo pipefail
          mkdir -p ~/.ssh
          printf '%s\n' "$PROD_SSH_KNOWN_HOSTS" > ~/.ssh/known_hosts
          chmod 644 ~/.ssh/known_hosts

      - name: Deploy app on server
        env:
          PROD_SSH_HOST: ${{ secrets.PROD_SSH_HOST }}
          PROD_SSH_USER: ${{ secrets.PROD_SSH_USER }}
          PROD_SSH_PORT: ${{ secrets.PROD_SSH_PORT }}
        run: |
          ssh -p "$PROD_SSH_PORT" "$PROD_SSH_USER@$PROD_SSH_HOST" << 'EOF'
          set -euo pipefail
          cd /opt/mcgfinance
          git fetch origin main
          git checkout main
          git reset --hard origin/main
          docker compose --profile prod up --build -d
          EOF
